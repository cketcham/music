<h1><%=h @song.name %></h1>

<div class="song">

<%= render :partial => 'song', :locals => { :song => @song, :autostart => true, :onend => "window.location.href = \"/songs/random\";" } %>
	<div class="right">
		<ul>
			<li>
			<h2>
			<%= link_to 'skip ⇢', :action => 'random' %>
			</h2>
			</li>
		</ul>
	</div>
</div>


<%= link_to 'Edit', edit_song_path(@song) %> |
<%= link_to 'Back', songs_path %>


